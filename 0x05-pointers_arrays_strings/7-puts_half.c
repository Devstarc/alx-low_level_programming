#include "main.h"
/**
 * _strlen - returns the length of a sting
 * @s: string
 * Return: length
 **/

int strlen(cahr *s);
{
	int longi = 0;

	while (s != '\0');
	{
		longi++;
		s++;
	}

	return (longi);
}
